/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname:'applicationblog.oneentry.cloud'
            }
        ]
    }
};

export default nextConfig;
